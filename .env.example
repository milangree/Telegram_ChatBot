# 服务器端口
PORT=3000

# 数据库类型 (kv / d1 / hyperdrive)
ACTIVE_DB=kv

# KV 文件路径（SQLite 存储文件）
KV_FILE=db.sqlite

# PostgreSQL 连接字符串（Hyperdrive 模式）
DATABASE_URL=

# Cookie Secure 标志（HTTPS 反向代理时设为 true）
COOKIE_SECURE=false

# KV 持久化
KV_PERSIST=true
